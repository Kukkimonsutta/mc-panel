import { Connection } from ".";
export interface ClientOptions {
    accessToken?: string;
    profile?: string;
    keepAlive?: boolean;
    /** @default 120000 ms */
    timeout?: number;
    /** @default 10000 ms */
    connectTimeout?: number;
}
export declare class Client extends Connection {
    static connect(host: string, port?: number | null, options?: ClientOptions): Promise<Client>;
    options: ClientOptions;
    constructor(options?: ClientOptions);
    connect(host: string, port?: number | null): Promise<this>;
    private stateChanged;
    private onEncryptionRequest;
}
