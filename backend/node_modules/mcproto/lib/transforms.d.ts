/// <reference types="node" />
import { Transform, TransformCallback } from "stream";
export declare class Writer extends Transform {
    compressionThreshold: number;
    _transform(chunk: Buffer, _enc: string, callback: TransformCallback): void;
}
export declare class Reader extends Transform {
    compressionThreshold: number;
    private buffer;
    private transforming;
    private flushCb?;
    _transform(chunk: Buffer, _enc: string, callback: TransformCallback): Promise<void>;
    flush(callback: () => void): void;
}
